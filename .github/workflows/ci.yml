name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install
        run: python -m pip install -e ".[dev]"
      - name: Schema generation
        run: |
          calibrex schema all --output-dir /tmp/calibrex-schemas
          test "$(find /tmp/calibrex-schemas -maxdepth 1 -name '*.schema.json' | wc -l)" -eq 20
      - name: Local release smoke
        run: |
          python tools/local_release_smoke.py \
            --venv /tmp/calibrex-wheel-smoke \
            --build-env /tmp/calibrex-wheel-build \
            --schema-dir /tmp/calibrex-wheel-schemas \
            --report-dir /tmp/calibrex-wheel-report
      - name: Upload package artifacts
        if: matrix.python-version == '3.12'
        continue-on-error: true
        uses: actions/upload-artifact@v4
        with:
          name: calibrex-dist
          path: dist/*
      - name: Test
        run: pytest
      - name: Ruff
        run: ruff check .
      - name: Install uv
        run: python -m pip install uv
      - name: Mypy
        run: uvx --with pydantic --with PyYAML --with jsonschema mypy src/calibrex

  opencv-adapter:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install OpenCV adapter
        run: python -m pip install -e ".[dev,opencv]"
      - name: Test OpenCV hand-eye conventions
        run: pytest tests/unit/test_opencv_hand_eye_adapters.py
