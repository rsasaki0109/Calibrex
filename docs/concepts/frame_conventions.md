# Frame Conventions

slac uses:

- `T_parent_child`: maps child-frame points into the parent frame.
- meters for length.
- radians internally for angles.
- `xyzw` quaternion order.
- integer nanoseconds as the standard timestamp representation.
- `sensor_time + dt_sensor = reference_time` for time offsets.
